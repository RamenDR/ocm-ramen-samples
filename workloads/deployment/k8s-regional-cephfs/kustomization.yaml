---
resources:
  - ../base
patches:
  - target:
      kind: PersistentVolumeClaim
      name: cephfs-pvc
    patch: |-
      - op: replace
        path: /spec/storageClassName
        value: rook-cephfs
