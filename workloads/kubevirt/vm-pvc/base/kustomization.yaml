---
resources:
  - source.yaml
  - pvc.yaml
  - vm.yaml
labels:
  - pairs:
      appname: vm
secretGenerator:
  - name: my-public-key
    files:
      - test_rsa.pub
generatorOptions:
  disableNameSuffixHash: true
